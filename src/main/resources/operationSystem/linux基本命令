创建文件: touch
创建软链： ln -s
修改owner: chown -R work:work xxx
观察8080端口是否存在：netstat -anp | grep 8080
杀死指定所有php进程：killall php
文件权限：
    640：6表示文件所有者有读写权限，没有执行权限；
         4表示用户组其他人有读权限，没有写和执行的权限。
         0表示其他人没有任何执行权限。
